# 🏗️ اتاق ساخت چک‌یار — برد کنترل (منبع حقیقت)

این فایل مغز پروژه است. هر تصمیم، تسک و وضعیت اینجا ثبت می‌شود. مدیر (کلود) این فایل را به‌روز نگه می‌دارد.

نسخه‌ی خوانا و بصری این برد به‌صورت داشبورد HTML هم ساخته شده («اتاق ساخت چک‌یار»).

---

## 🎯 هدف

افزودن همه‌ی قابلیت‌های نقشه‌راه به چک‌یار + ساخت نسخه‌ی اندروید، از طریق یک خط‌تولید چند-ایجنتی.

---

## 👥 نقش‌ها

| نقش | کیست | مسئولیت |
|-----|------|---------|
| **مدیر / یکپارچه‌ساز** | کلود (همین‌جا) | معماری، تقسیم تسک، نوشتن کارت دستور دقیق، ادغام کد همه در `index.html`، تست با مرورگر headless، نوشتن فایل نهایی روی مخزن، به‌روزرسانی این برد. تنها کسی که مستقیم روی فایل مخزن می‌نویسد. |
| **مجری کدنویسی** | Codex (وب) | پیاده‌سازی تسک‌های مشخص با اسپک دقیق — مدل داده، منطق، دیف‌های چندبخشی. |
| **مجری بافت‌بزرگ** | Google AI Studio / Gemini (وب) | تبدیل‌های کل‌فایل و کارهای بصری/CSS که به دیدِ کل فایل نیاز دارند. |
| **رله + بازبین** | شفیع (تو) | کارت دستور را در ابزار درست پیست می‌کنی، خروجی را برمی‌گردانی، و در VS Code / GitHub Desktop نتیجه را بازبینی و کامیت می‌کنی. |

**قانون طلایی:** فقط مدیر (کلود) روی `index.html`ِ مخزن می‌نویسد. Codex و Gemini فقط کد *تولید* می‌کنند؛ اعمال و تستش با کلود است تا فایل همیشه سالم و یکدست بماند و تاریخچه‌ی گیت تمیز بماند.

---

## 🔁 حلقه‌ی کار (پایپلاین)

```
[۱ اسپک]      کلود کارت دستور را می‌سازد  →  cards/card-XX-*.md یا .txt
     ↓
[۲ تولید]     تو کارت را در Codex یا AI Studio پیست می‌کنی، خروجی را می‌گیری
     ↓
[۳ تحویل]     خروجی را به کلود برمی‌گردانی (پیست در همین چت یا فایل)
     ↓
[۴ ادغام]     کلود کد را در index.html ادغام می‌کند
     ↓
[۵ تست]       کلود با مرورگر headless رندر و بررسی می‌کند (اسکرین‌شات + چک منطق)
     ↓
[۶ تحویل]     کلود فایل نهایی را روی مخزن تو می‌نویسد → تو در GitHub Desktop کامیت می‌کنی
```

هر بار **یک کارت در جریان** است تا ادغام و تست تمیز بماند (چون همه یک فایل واحد را دست می‌زنند). بعد از پایدار شدن حلقه، می‌توانیم موازی هم برویم.

---

## 🧩 فرمت کارت دستور

هر کارت این بخش‌ها را دارد:
- **شناسه و مجری:** مثلاً `CARD-01 · Gemini`
- **هدف:** یک جمله
- **ورودی:** فایل/کد فعلی که باید کار روی آن انجام شود
- **کار دقیق:** لیست تغییرات مورد انتظار
- **قیود:** چیزهایی که نباید بشکند
- **خروجی مورد انتظار:** دقیقاً چه چیزی برگردانده شود (کل فایل / فقط بلوک‌ها)

---

## 📋 بک‌لاگ و وضعیت

| # | تسک | مجری | وضعیت |
|---|------|------|-------|
| S | راه‌اندازی سیستم و برد | کلود | ⏳ در حال انجام |
| 1 | حالت تاریک (Dark Mode) | Gemini | 🟡 کارت آماده — منتظر اجرا |
| 2 | وضعیت هر چک (در انتظار/وصول/برگشت) | Codex | ⬜ در نوبت |
| 3 | دسته‌بندی دفتر بر اساس مشتری | Codex | ⬜ در نوبت |
| 4 | محاسبه‌گر خسارت تأخیر تأدیه | کلود | ⬜ در نوبت (نیاز به تحقیق فرمول) |
| 5 | بسته‌بندی اندروید (Capacitor) | کلود + تو | ⬜ در نوبت |
| 6 | تست نهایی و انتشار | کلود | ⬜ در نوبت |

**علائم:** ✅ تمام · ⏳/🟡 در جریان · ⬜ در نوبت · 🔴 مشکل

---

## 🗒️ گزارش کار (Changelog)

- `1405-05-09` — سیستم ارکستریشن راه‌اندازی شد؛ مخزن چک‌یار بررسی و معماری استخراج شد؛ کارت اول (حالت تاریک) برای Gemini ساخته شد.
